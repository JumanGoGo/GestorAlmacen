<UserControl x:Class="GestorAlmacen.Views.MovimientosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="900" Background="White">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Vertical">
                <TextBlock x:Name="txtTitulo" Text="Historial de Movimientos" FontSize="24" FontWeight="Bold" Foreground="#333"/>
                <TextBlock Text="Consulta y auditoría de operaciones" Foreground="Gray" FontSize="12"/>
            </StackPanel>

            <Button Grid.Column="1" Content="+ Registrar Movimiento" 
                    Background="#007ACC" Foreground="White" FontWeight="Bold" 
                    Padding="15,8" Height="35" Click="btnNuevoMovimiento_Click"/>
        </Grid>

        <Border Grid.Row="1" Background="#F9F9F9" BorderBrush="#EEE" BorderThickness="1" Padding="10" Margin="0,0,0,15">
            <StackPanel Orientation="Horizontal">

                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Buscar Folio:" FontSize="10" FontWeight="SemiBold"/>
                    <TextBox x:Name="txtBuscar" Width="120" Padding="5" Tag="Ej. ENTR-001"/>
                </StackPanel>

                <StackPanel x:Name="pnlFiltroTipo" Margin="0,0,15,0">
                    <TextBlock Text="Tipo:" FontSize="10" FontWeight="SemiBold"/>
                    <ComboBox x:Name="cmbFiltroTipo" Width="100" Padding="5" SelectionChanged="Filtro_Changed">
                        <ComboBoxItem Content="Todos" IsSelected="True"/>
                        <ComboBoxItem Content="Entrada" Tag="ENTR"/>
                        <ComboBoxItem Content="Salida" Tag="SAL"/>
                        <ComboBoxItem Content="Transferencia" Tag="TRANS"/>
                        <ComboBoxItem Content="Ajuste" Tag="AJU"/>
                    </ComboBox>
                </StackPanel>

                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Desde:" FontSize="10" FontWeight="SemiBold"/>
                    <DatePicker x:Name="dpDesde" Width="100"/>
                </StackPanel>
                <StackPanel Margin="0,0,15,0">
                    <TextBlock Text="Hasta:" FontSize="10" FontWeight="SemiBold"/>
                    <DatePicker x:Name="dpHasta" Width="100"/>
                </StackPanel>

                <Button Content="Filtrar" Height="26" VerticalAlignment="Bottom" Padding="10,0" Click="btnFiltrar_Click"/>
            </StackPanel>
        </Border>

        <DataGrid x:Name="dgMovimientos" Grid.Row="2" AutoGenerateColumns="False" IsReadOnly="True" 
                  CanUserAddRows="False" SelectionMode="Single" BorderBrush="#DDD" BorderThickness="1">
            <DataGrid.Columns>
                <DataGridTextColumn Header="Folio" Binding="{Binding Folio}" Width="100" FontWeight="Bold"/>
                <DataGridTextColumn Header="Fecha" Binding="{Binding Fecha, StringFormat=dd/MM/yyyy HH:mm}" Width="120"/>
                <DataGridTextColumn Header="Tipo" Binding="{Binding Tipo}" Width="100"/>
                <DataGridTextColumn Header="Usuario" Binding="{Binding Usuario}" Width="120"/>
                <DataGridTextColumn Header="Estado" Binding="{Binding Estatus}" Width="80"/>
                <DataGridTextColumn Header="Comentario" Binding="{Binding Comentario}" Width="*"/>

                <DataGridTemplateColumn Width="90">
                    <DataGridTemplateColumn.CellTemplate>
                        <DataTemplate>
                            <Button Content="Ver Detalle" FontSize="10" Padding="5,2" Click="btnVerDetalle_Click"/>
                        </DataTemplate>
                    </DataGridTemplateColumn.CellTemplate>
                </DataGridTemplateColumn>
            </DataGrid.Columns>
        </DataGrid>

        <StatusBar Grid.Row="3" Margin="0,10,0,0">
            <StatusBarItem>
                <TextBlock Text="Mostrando últimos 50 registros" FontStyle="Italic" FontSize="11"/>
            </StatusBarItem>
        </StatusBar>
    </Grid>
</UserControl>