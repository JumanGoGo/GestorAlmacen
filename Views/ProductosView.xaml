<UserControl x:Class="GestorAlmacen.Views.ProductosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White" Foreground="#2D3E50">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Margin="0,0,0,15">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal">
                <TextBlock Text="Catálogo de Productos" FontSize="24" FontWeight="Bold" VerticalAlignment="Center" Foreground="#333"/>
            </StackPanel>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <TextBox x:Name="txtBuscar" Width="200" Padding="5" VerticalContentAlignment="Center"  Tag="Buscar SKU o Nombre..." />
                <Button x:Name="btnBuscar" Content="Buscar" Margin="5,0,0,0" Padding="10,5" Click="btnBuscar_Click"/>
            </StackPanel>
        </Grid>

		<DataGrid x:Name="dgProductos" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True"
		   CanUserAddRows="False" SelectionMode="Single"
		   BorderBrush="#DDD" BorderThickness="1"
		   RowBackground="White" AlternatingRowBackground="#F8F9FA">

			<DataGrid.Resources>
				<Style TargetType="DataGridCell">
					<Setter Property="Foreground" Value="#2D3E50"/>
					<Style.Triggers>
						<Trigger Property="IsSelected" Value="True">
							<Setter Property="Background" Value="#007ACC"/>
							<Setter Property="Foreground" Value="White"/>
						</Trigger>
					</Style.Triggers>
				</Style>
			</DataGrid.Resources>

			<DataGrid.Columns>
				<DataGridTextColumn Header="ID" Binding="{Binding product_id}" Width="50"/>

				<DataGridTextColumn Header="SKU" Binding="{Binding sku}" Width="100" FontWeight="Bold"/>

				<DataGridTextColumn Header="Nombre" Binding="{Binding name}" Width="*"/>

				<DataGridTextColumn Header="Categoría" Binding="{Binding Category.name}" Width="150"/>

				<DataGridTextColumn Header="Stock Global" Binding="{Binding StockCalculado}" Width="100"/>

				<DataGridCheckBoxColumn Header="Activo" Binding="{Binding is_active}" Width="60"/>
			</DataGrid.Columns>
		</DataGrid>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,15,0,0">
            <Button x:Name="btnNuevo" Content="Nuevo Producto" Padding="15,8" Background="#007ACC" Foreground="White" FontWeight="Bold" Margin="0,0,10,0" Click="btnNuevo_Click"/>
            <Button x:Name="btnEditar" Content="Editar" Padding="15,8" Margin="0,0,10,0" Click="btnEditar_Click"/>
            <Button x:Name="btnEliminar" Content="Desactivar" Padding="15,8" Background="#E74C3C" Foreground="White" Click="btnEliminar_Click"/>
        </StackPanel>
    </Grid>
</UserControl>