<UserControl x:Class="GestorAlmacen.Views.CategoriasView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800" Background="White" Foreground="Black">
	

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <!-- Lista -->
            <ColumnDefinition Width="1*"/>
            <!-- Formulario -->
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Grid.ColumnSpan="2" Text="Catálogo de Categorías" FontSize="22" FontWeight="Bold" Foreground="#333" Margin="0,0,0,20"/>

        <!-- SECCIÓN IZQUIERDA: LISTA -->
        <Grid Grid.Row="1" Grid.Column="0" Margin="0,0,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Buscador -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBox x:Name="txtBuscar" Width="200" Padding="5" Tag="Buscar categoría..." TextChanged="txtBuscar_TextChanged"/>
                <TextBlock Text="🔍" VerticalAlignment="Center" Margin="5,0,0,0"/>
            </StackPanel>

            <!-- Tabla -->
            <DataGrid x:Name="dgCategorias" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                      CanUserAddRows="False" SelectionMode="Single" BorderBrush="#DDD" BorderThickness="1"
                      SelectionChanged="dgCategorias_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding category_id}" Width="50"/>
                    <DataGridTextColumn Header="Nombre" Binding="{Binding name}" Width="150" FontWeight="Bold"/>
                    <DataGridTextColumn Header="Descripción" Binding="{Binding description}" Width="*"/>
					<DataGridTemplateColumn Header="Estado" Width="80">
						<DataGridTemplateColumn.CellTemplate>
							<DataTemplate>
								<TextBlock FontSize="12" FontWeight="Bold" HorizontalAlignment="Center">
									<TextBlock.Style>
										<Style TargetType="TextBlock">
											<!-- Valor por defecto: Inactivo -->
											<Setter Property="Text" Value="INACTIVA"/>
											<Setter Property="Foreground" Value="Red"/>

											<Style.Triggers>
												<DataTrigger Binding="{Binding is_active}" Value="True">
													<Setter Property="Text" Value="ACTIVA"/>
													<Setter Property="Foreground" Value="Green"/>
												</DataTrigger>
											</Style.Triggers>
										</Style>
									</TextBlock.Style>
								</TextBlock>
							</DataTemplate>
						</DataGridTemplateColumn.CellTemplate>
					</DataGridTemplateColumn>

				</DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- SECCIÓN DERECHA: EDICIÓN -->
        <Border Grid.Row="1" Grid.Column="1" Background="#F9F9F9" BorderBrush="#EEE" BorderThickness="1" Padding="15" CornerRadius="5" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="Datos de la Categoría" FontWeight="Bold" FontSize="14" Margin="0,0,0,15"/>

                <TextBlock Text="Nombre: *" Margin="0,0,0,5"/>
                <TextBox x:Name="txtNombre" Padding="5" MaxLength="100" Margin="0,0,0,10"/>

                <TextBlock Text="Descripción:" Margin="0,0,0,5"/>
                <TextBox x:Name="txtDescripcion" Padding="5" Height="60" TextWrapping="Wrap" AcceptsReturn="True" Margin="0,0,0,10"/>

                <CheckBox x:Name="chkActivo" Content="Categoría Activa" IsChecked="True" Margin="0,0,0,20"/>

                <!-- Botones Acción -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="btnLimpiar" Content="Nuevo / Limpiar" Margin="0,0,5,0" Click="btnLimpiar_Click" Background="White"/>
                    <Button x:Name="btnGuardar" Grid.Column="1" Content="Guardar" Background="#007ACC" Foreground="White" FontWeight="Bold" Click="btnGuardar_Click"/>
                </Grid>

                <Button x:Name="btnEliminar" Content="Desactivar" Background="#E74C3C" Foreground="White" Margin="0,10,0,0" Click="btnEliminar_Click" Visibility="Collapsed"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>