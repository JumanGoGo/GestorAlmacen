<UserControl x:Class="GestorAlmacen.Views.UsuariosView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             mc:Ignorable="d" 
             d:DesignHeight="500" d:DesignWidth="900" Background="White">

    <Grid Margin="20">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <!-- Lista -->
            <ColumnDefinition Width="1.2*"/>
            <!-- Formulario -->
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Título -->
        <TextBlock Grid.ColumnSpan="2" Text="Administración de Usuarios y Accesos" FontSize="22" FontWeight="Bold" Foreground="#333" Margin="0,0,0,20"/>

        <!-- SECCIÓN IZQUIERDA: LISTA -->
        <Grid Grid.Row="1" Grid.Column="0" Margin="0,0,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <!-- Buscador -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBox x:Name="txtBuscar" Width="200" Padding="5" Tag="Buscar usuario..." TextChanged="txtBuscar_TextChanged"/>
                <TextBlock Text="🔍" VerticalAlignment="Center" Margin="5,0,0,0"/>
            </StackPanel>

            <!-- Tabla -->
            <DataGrid x:Name="dgUsuarios" Grid.Row="1" AutoGenerateColumns="False" IsReadOnly="True" 
                      CanUserAddRows="False" SelectionMode="Single" BorderBrush="#DDD" BorderThickness="1"
                      SelectionChanged="dgUsuarios_SelectionChanged">
                <DataGrid.Columns>
                    <DataGridTextColumn Header="ID" Binding="{Binding user_id}" Width="40"/>
                    <DataGridTextColumn Header="Usuario (Login)" Binding="{Binding username}" Width="100" FontWeight="Bold"/>
                    <DataGridTextColumn Header="Nombre Completo" Binding="{Binding display_name}" Width="*"/>
                    <DataGridTextColumn Header="Rol" Binding="{Binding role}" Width="90"/>
                    <DataGridTemplateColumn Header="Estado" Width="70">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>
                                <TextBlock Text="{Binding is_active}" Foreground="Gray"/>
                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>
                </DataGrid.Columns>
            </DataGrid>
        </Grid>

        <!-- SECCIÓN DERECHA: FORMULARIO -->
        <Border Grid.Row="1" Grid.Column="1" Background="#F9F9F9" BorderBrush="#EEE" BorderThickness="1" Padding="20" CornerRadius="5" VerticalAlignment="Top">
            <StackPanel>
                <TextBlock Text="Datos del Usuario" FontWeight="Bold" FontSize="14" Margin="0,0,0,15"/>

                <TextBlock Text="Nombre Completo:" Margin="0,0,0,5"/>
                <TextBox x:Name="txtDisplayName" Padding="5" Margin="0,0,0,10"/>

                <TextBlock Text="Usuario (Login): *" Margin="0,0,0,5"/>
                <TextBox x:Name="txtUsername" Padding="5" Margin="0,0,0,10" CharacterCasing="Lower"/>

                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>

                    <!-- Columna 1: Password -->
                    <StackPanel Grid.Column="0" Margin="0,0,5,0">
                        <TextBlock Text="Contraseña:" Margin="0,0,0,5"/>
                        <PasswordBox x:Name="txtPassword" Padding="5" Margin="0,0,0,5"/>
                        <TextBlock x:Name="lblPasswordHint" Text="* Dejar vacío para no cambiar" FontSize="9" Foreground="Gray" Visibility="Collapsed"/>
                    </StackPanel>

                    <!-- Columna 2: Rol -->
                    <StackPanel Grid.Column="1" Margin="5,0,0,0">
                        <TextBlock Text="Rol Asignado: *" Margin="0,0,0,5"/>
                        <ComboBox x:Name="cmbRol" Padding="5" Margin="0,0,0,10">
                            <ComboBoxItem Content="ADMIN"/>
                            <ComboBoxItem Content="SUPERVISOR"/>
                            <ComboBoxItem Content="OPERADOR"/>
                        </ComboBox>
                    </StackPanel>
                </Grid>

                <Separator Margin="0,10"/>

                <CheckBox x:Name="chkActivo" Content="Usuario Activo (Permitir acceso)" IsChecked="True" Margin="0,0,0,20"/>

                <!-- Botones Acción -->
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <Button x:Name="btnLimpiar" Content="Nuevo / Limpiar" Margin="0,0,5,0" Click="btnLimpiar_Click" Background="White" Height="30"/>
                    <Button x:Name="btnGuardar" Grid.Column="1" Content="Guardar Usuario" Background="#27AE60" Foreground="White" FontWeight="Bold" Click="btnGuardar_Click" Height="30"/>
                </Grid>

                <Button x:Name="btnEliminar" Content="Inhabilitar Acceso" Background="#E74C3C" Foreground="White" Margin="0,10,0,0" Click="btnEliminar_Click" Visibility="Collapsed" Height="30"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>